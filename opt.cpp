/*	Le Problème

  étant donné un ensemble de nombres, trouver la combinaison d'une partie d'entre eux
  qui optimise leur somme au plus près d'un autre nombre sans le dépasser.

*/
#include "transfo_bin.h"


void main(){/*
	int i=0,j,limite,r;

	//Declaration des variables necessaires au programme 
char adresse_du_fichier[100];
ifstream InFile;
double tmp,canpd;
double tab[11];
int taille_liste;
int mot_bin[50];
char* liste_optimum[50];
double somme;

	// on passera l'ensemble des valeurs des nombres dans un fichier ensemble.txt
cout<<"entrez l'adresse du fichier contenant la liste"<<'\n';
cin>>adresse_du_fichier; 
cout<<'\n';

InFile.open(adresse_du_fichier);// ici F:\optimisation\ensemble.txt

while (!InFile.eof()){
	InFile>>tmp;
	cout<<tmp<<'\n';
	tab[i]=tmp;
	i++;
	}

InFile.close();
taille_liste=i;

	// on demandera la valeur à ne pas dépasser dans une boite de dialogue
cout<<'\n'
	<<"quel est le chiffre a ne pas depasser ?"<<'\n';
cin>>canpd;
cout<<'\n';

	// il faut transformer chaque chiffre en son expression binaire codée sur taille_liste bits
	// mais il faut s'arreter si on dépasse canpd
*/
	int i=pow(4,44);
	cout<<i<<"\n\n"<<restee(i,10)<<"\n\n";

}

